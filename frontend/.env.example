# Copy to .env.local for development.

# Base URL of the backend API (no trailing slash). Must be reachable from the
# browser and allowed in the backend's CORS_ORIGIN.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
